// Package config contains the configuration required to run a node.
// while each submodule defines it's own validator adhering to common.Validator
// they are all stored and validated centrally bt the node here.
package config
