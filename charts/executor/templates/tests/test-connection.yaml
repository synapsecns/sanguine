apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "executor.fullname" . }}-test-connection"
  labels:
    {{- include "executor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "executor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
