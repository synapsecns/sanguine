apiVersion: v1
kind: Service
metadata:
  name: {{ include "executor.fullname" . }}
  labels:
    {{- include "executor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "executor.selectorLabels" . | nindent 4 }}
    executor-type: remote
