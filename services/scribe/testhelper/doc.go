// Package testhelper makes it easy for other packages requiring scribe to construct one using simulated backends.
// as a reminder, scribe requires rpc addresses, so a backend that exposes one is required.
package testhelper
