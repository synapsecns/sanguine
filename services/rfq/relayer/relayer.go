package relayer

import (
	"context"
)

// Relayer is the core of the relayer application
type Relayer struct {
	cfg config.Config
}

// NewRelayer creates a new relayer.
func NewRelayer(ctx context.Context, cfg config.Config) {

}
