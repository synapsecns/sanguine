chains:
  1: 'chain_config_1'
  2: 'chain_config_2'
omnirpc_url: 'http://example.com/rpc'
database:
  type: 'sqlite'
  dsn: 'username:password@tcp(localhost:3306)/database_name'
signer:
  # signer config details here
submitter_config:
  # submitter config details here
should_relay: true
bootstrap_peers:
  - 'peer1.example.com'
  - 'peer2.example.com'
p2p_port: 12345
use_peer_id: false
